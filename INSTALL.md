Afin d'utiliser ce script, il utiliser anaconda et créer un environnement :

```bash
conda create -n vgg_face python=3.9
conda activate vgg_face
```

L'ensemble des dépendances se trouve dans le fichier requirements.txt.
Pour les installer, il suffit d'utiliser pip :

```bash
pip install -r requirements.txt
```